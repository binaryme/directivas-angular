import { OtradirectivaDirective } from './otradirectiva.directive';

describe('OtradirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new OtradirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
