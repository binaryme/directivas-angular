import { EjmidirectivaDirective } from './ejmidirectiva.directive';

describe('EjmidirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new EjmidirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
